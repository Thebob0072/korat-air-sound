# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรง
# cp .env.example .env

# ── ตัวเลือก 1: ใช้ Cloud SQL (Production) ────────────────────────────────────
# DATABASE_URL="mysql://root:P%40ssw0rd@34.158.34.228:3306/korat_air_sound"

# ── ตัวเลือก 2: ใช้ Local MySQL ผ่าน docker-compose ──────────────────────────
DATABASE_URL="mysql://root:korat_pass@localhost:3306/korat_air_sound"

PORT=3001
NODE_ENV=development
